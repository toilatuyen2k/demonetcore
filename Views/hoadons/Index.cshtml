@model IEnumerable<DemoNetcore.Models.hoadon>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.hoadonCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.hoadonFullName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.hoadonCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.hoadonFullName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.hoadonId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.hoadonId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.hoadonId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
